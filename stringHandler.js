// GENERAL
const general = require("./strings/general");

// SETUP
const setup = require("./strings/setup");

// ERRORS
const errors = require("./strings/errors");

module.exports = {
    general,
    setup,
    errors
};